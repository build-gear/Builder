# Build Plan

Convert a broad product goal into a scoped build plan with implementation order, verification steps, and concrete artifacts.

Use Codex through Builder Gear's run request contract. Keep authentication user-owned and never request or print auth file contents.

